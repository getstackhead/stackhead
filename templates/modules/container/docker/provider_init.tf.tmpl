# This file was generated by StackHead. Do not modify it.

provider "docker" {
  alias = "{{$.Context.Project.Name}}"

  {{ range $index, $registry := $.Context.Project.Container.Registries }}
  registry_auth {
    address = "{{ or $registry.Url "registry.hub.docker.com" }}"
    username = "{{ $registry.Username }}"
    password = "{{ $registry.Password }}"
  }
  {{ end }}
}
