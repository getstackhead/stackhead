---
domains:
  - domain: schema.stackhead.io
    expose:
      - service: nginx
        internal_port: 80
        external_port: 80
container:
  services:
    - name: data
      image: getstackhead/schemas:latest
    - name: nginx
      image: getstackhead/nginx:standalone
      #hooks:
      #  after_setup:
      #    # add autoindex to list all schema files
      #    - sed -i "s/location \/ {/location \/ {\n       autoindex on;/" /etc/nginx/conf.d/default.conf
      volumes_from:
        - data
