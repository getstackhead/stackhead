{
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/exec",
      {"prepareCmd": "echo \"v${nextRelease.version}\" > VERSION && echo \"v${nextRelease.version}\" > ansible/VERSION && (cd schemas && find . -name \"*.json\" -exec cp --parents -R '{}' \"../ansible/schemas/\" ';')"}
    ],
    [
      "@semantic-release/git",
      {"assets": ["VERSION", "ansible/VERSION"] }
    ],
    "@semantic-release/github"
  ],
  "branches": [
    "master"
  ]
}
